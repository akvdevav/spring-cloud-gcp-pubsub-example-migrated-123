---
applications:
- name: spring-cloud-gcp-pubsub-example-modernized
  memory: 1G
  instances: 1
  buildpacks:
    - java_buildpack
  path: build/libs/*.jar
  env:
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 21.+ } }'
    SPRING_PROFILES_ACTIVE: cloud

